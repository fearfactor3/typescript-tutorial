name: aws-ts-eks-hello-world
description: EKS and Kubernetes Hello World example
runtime: nodejs
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
